{
  "module_type": "core",
  "mixins": ["canRespawn"],
    "init": function() {
    //This class handles all styles that are used in
    //layouts and stuff. There is still a little css
    //in the html, but it should really all move in here

    //inner tables
    jss(".innertable", {
      "border-collapse": "collapse",
        "text-align": "center",
        "border": "solid black 1px"
    });

    jss(".innertd", {
      "border": "solid black 1px",
      padding: "5px"
    });
    jss(".innertddisplay", {
      padding: "20px"
    });

    jss(".login_input", {
      border: "solid gray 1px",
        "margin-top": "5px",
        "margin-bottom": "5px",
      width: "100px",
      padding: "2px"
    });
  }
}
